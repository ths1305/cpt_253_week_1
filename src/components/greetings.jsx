import React from 'react';

// TODO: Update this component to utilize the variable passed to it!
//"World" is now {name} as a placeholder

export const Greeting = ({ name }) => {

    return (
        <div>
            <h1>Hello, {name}!</h1>
        </div>
    )
}
